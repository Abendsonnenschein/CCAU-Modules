import { addDatesButton } from "./dh_add";
import { addMoveButton } from "./mc_move";
import { addRemoveButton } from "./mc_remove";

function addButtons() {
  addDatesButton();
  addRemoveButton();
  addMoveButton();
}

addButtons();
